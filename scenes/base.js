class sceneBase {
  canvasWidth = 32;
  canvasHeight = 32;
  p;

  constructor(w, h, p){
    this.canvasWidth = w;
    this.canvasHeight = h;
    this.p = p;
  }  

  setup() {
  }
  
  draw() {
  }
}